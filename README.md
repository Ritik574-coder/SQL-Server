# SQL Server & SQL Learning Repository

Welcome to my **SQL Server & SQL Learning Repo** 🎉  
This repository contains scripts, notes, and examples that cover everything from beginner concepts to advanced data engineering practices.

---

## 📚 Contents

### 1. SQL Basics
- `SELECT`, `INSERT`, `UPDATE`, `DELETE`
- Filtering with `WHERE`, `LIKE`, `IN`, `BETWEEN`
- Sorting and limiting results
- Aggregate functions (`COUNT`, `SUM`, `AVG`, `MIN`, `MAX`)
- Grouping with `GROUP BY` and filtering with `HAVING`

### 2. Joins & Relationships
- INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL JOIN
- Self joins
- Cross joins
- Foreign keys and referential integrity

### 3. Constraints & Indexes
- Primary keys, foreign keys
- Unique constraints
- Check constraints
- Clustered vs Non‑clustered indexes
- Performance tuning with indexes

### 4. Advanced SQL Server Features
- Identity columns
- Default values
- Views
- Stored Procedures
- Functions (Scalar & Table‑valued)
- Triggers (AFTER INSERT/UPDATE/DELETE)

### 5. Bulk Data Import
- `BULK INSERT` examples
- Import via staging tables
- High‑performance import using views
- Batch size tuning (`BATCHSIZE`, `ROWS_PER_BATCH`)
- Recovery model adjustments for big loads

### 6. Performance & Optimization
- Query execution plans
- Index strategies
- Partitioning large tables
- Statistics and maintenance
- Minimizing transaction log growth during bulk loads

### 7. Real‑World Scenarios
- Customer table design with constraints
- Bulk import of 50M+ rows
- Handling identity columns during import
- Error handling with `ERRORFILE` and `MAXERRORS`

---

## 🚀 How to Use
1. Clone the repo:
   ```bash
   https://github.com/Ritik574-coder
